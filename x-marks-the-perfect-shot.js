const moves = ['north', 'north', 'west', 'west', 'north', 'east','north']; 
const finalPosition = function (moves) {
  // Your code in here ...
for(let move of moves){

  if( move === 'north'){ 
    
    return move[i].length += 1;
   }
  
}
  
  
};

finalPosition(north);